# Pinned to the versions this application is tested against, so a rebuild in
# six months installs what runs today rather than whatever is newest.
# To move a version, change it here and run the tests.
fastapi==0.138.0
uvicorn==0.49.0
pandas==3.0.3
openpyxl==3.1.5
jinja2==3.1.6
requests==2.34.2
beautifulsoup4==4.15.0
playwright==1.60.0
pypdf==6.14.2
cryptography==49.0.0
certifi==2026.6.17
