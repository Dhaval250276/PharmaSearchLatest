services:
  - type: web
    name: pharmasearchlatest
    env: python
    plan: free
    rootDir: Backend
    buildCommand: pip install -r requirements.txt && playwright install chromium
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
